# TransBridge

TransBridge is a lightweight user-space middleware that transparently bridges traditional TCP socket applications with high-performance RoCE communication.  
It allows unmodified socket-based applications to achieve lower latency by bypassing kernel and protocol stack overhead.  
TransBridge is designed for latency-sensitive scenarios such as edge intelligence, industrial analytics, and large-scale sensor data processing.
